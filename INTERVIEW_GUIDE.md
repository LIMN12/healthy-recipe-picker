# Healthy Recipe Picker 项目说明与面试问答

## 项目内容

这是一个减脂菜谱随机推荐页。数据以 JavaScript 对象数组保存，页面会显示菜名、标签、估算营养数据、食材、做法、时间和难度。

## 我做了什么

- 为菜谱设计统一数据结构，减少重复 HTML。
- 将抽取结果渲染到各个页面区域。
- 记录上一次结果，避免连续显示同一道菜。
- 完成移动端适配、键盘焦点与减少动画适配。
- 为营养数据增加清晰的健康声明。

## 完整使用说明

1. 打开在线演示，或下载仓库后打开 `index.html`。
2. 阅读当前菜谱的营养估算、食材、步骤、耗时和难度。
3. 点击右下角的换一道按钮，展示另一道菜。
4. 连续点击检查程序不会立即重复上一道菜。
5. 在手机或缩小浏览器窗口后查看自适应布局。

**常见问题：** 项目不需要网络或安装依赖。营养数据是估算值，不能作为医疗或个性化饮食建议。

## 面试官可能问什么

### 为什么用对象数组

每道菜都有相同字段，使用对象数组能让数据和页面结构分离。增加菜谱时只需增加一个对象，不需要复制一整块 HTML。

### 怎么避免连续抽到同一道菜

我保存上一个索引。如果新生成的索引与它相同，就重新生成，直到不同为止。

### 这个项目的数据库在哪里

当前没有真正数据库，数据写在前端代码中。如果菜谱数量变多，我会把数据放到后端数据库，前端通过 API 获取。

### 为什么要写健康声明

页面中的营养值是估算，不能冒充专业医疗或营养建议。这体现了对产品边界和用户风险的思考。

### AI 生成的内容如何验证

我会检查数据字段是否完整，用多次点击检查随机逻辑，再用移动端尺寸检查排版。对无法保证的营养值明确标注为估算。

## 演示顺序

1. 介绍一道菜的数据字段。
2. 连续点击换一道，展示随机与防重复逻辑。
3. 缩小窗口，展示响应式布局。
4. 打开 README，说明使用方式与项目边界。
